export default function WorkboardContent() {
  return (
    <div className="p-6">
      <h1 className="text-lg font-semibold">Workflow</h1>
      <p className="text-sm text-muted-foreground">
        Minimal test — if you see this, the shell works.
      </p>
    </div>
  );
}
